[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "dev-coding-agent"
version = "0.1.0"
description = "A local-first, approval-gated AI coding agent."
readme = "README.md"
license = { file = "LICENSE" }
license-files = ["LICENSE"]
requires-python = ">=3.11"
dependencies = ["typer>=0.12", "textual>=0.70", "prompt-toolkit>=3.0"]

[project.optional-dependencies]
agent = ["langchain>=1.0", "langgraph>=1.0", "langgraph-checkpoint-sqlite>=2.0", "aiosqlite>=0.20", "langchain-openai>=1.0"]
tui = ["textual>=0.70"]
cli = ["typer>=0.12"]
openai = ["langchain-openai>=1.0"]
anthropic = ["langchain-anthropic>=1.0"]
azure = ["langchain-openai>=1.0"]
google = ["langchain-google-genai>=2.0"]
embeddings = ["numpy>=1.26", "onnxruntime>=1.18", "transformers>=4.40", "sentence-transformers>=3.0"]
tracing = ["langsmith>=0.2"]
dev = ["pytest>=8", "pytest-cov>=5", "ruff>=0.6", "pyright>=1.1"]
all = ["langchain>=1.0", "langgraph>=1.0", "langgraph-checkpoint-sqlite>=2.0", "aiosqlite>=0.20", "langchain-openai>=1.0", "langchain-anthropic>=1.0", "langchain-google-genai>=2.0", "textual>=0.70", "typer>=0.12", "numpy>=1.26", "onnxruntime>=1.18", "transformers>=4.40", "sentence-transformers>=3.0", "langsmith>=0.2"]

[project.scripts]
dev = "dev.cli.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/dev"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-markers"
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pyright]
include = ["src", "tests"]
typeCheckingMode = "basic"
